spring:
  datasource:
    url: jdbc:h2:mem:credential;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false
  application:
    name: credential-service-test

logging:
  level:
    pt.ulusofona: INFO
    org.apache.kafka: WARN
