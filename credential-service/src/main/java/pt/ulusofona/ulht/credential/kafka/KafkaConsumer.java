package pt.ulusofona.ulht.credential.kafka;

import org.springframework.stereotype.Service;

@Service
public class KafkaConsumer {

/*    private final Logger log = org.slf4j.LoggerFactory.getLogger(KafkaConsumer.class);

    @KafkaListener(topics = "${kafka.transaction-topic}", containerFactory = "kafkaListenerContainerFactory", groupId = "${spring.kafka.consumer.group-id}")
    public void listen(Message<LusofonaUser> transactionEventMessage) {
        System.out.println("Received message: " + transactionEventMessage.getPayload());
    }*/
}
