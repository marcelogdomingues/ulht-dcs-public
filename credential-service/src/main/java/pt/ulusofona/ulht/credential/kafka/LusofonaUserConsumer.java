package pt.ulusofona.ulht.credential.kafka;

import org.springframework.stereotype.Service;

@Service
public class LusofonaUserConsumer {
//
//    @KafkaListener(topics = "${kafka.transaction-topic}", containerFactory = "kafkaListenerContainerFactory", groupId = "${spring.kafka.consumer.group-id}")
//    public void listen(Message<LusofonaUser> lusofonaUserMessage) {
//        System.out.println("Received message: " + lusofonaUserMessage.getPayload());
//    }
//
//    @KafkaListener(topics = "${kafka.user-topic}", containerFactory = "kafkaListenerContainerFactory", groupId = "${spring.kafka.consumer.group-id}")
//    public void listenUser(Message<LusofonaUser> lusofonaUserMessage) {
//        System.out.println("Received user message: " + lusofonaUserMessage.getPayload());
//    }

}