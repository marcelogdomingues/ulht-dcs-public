# baseUrl advertised by the issuer in OID4VCI credential offers / metadata.
# Must be reachable by the wallet that redeems the offer. In this bundled stack
# the credential-service and walt.id share the `docker-compose_default` network,
# so the in-network hostname is used.
baseUrl = "http://issuer-api:7002"
