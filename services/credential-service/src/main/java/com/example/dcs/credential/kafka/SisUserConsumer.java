package com.example.dcs.credential.kafka;

import org.springframework.stereotype.Service;

@Service
public class SisUserConsumer {
//
//    @KafkaListener(topics = "${kafka.transaction-topic}", containerFactory = "kafkaListenerContainerFactory", groupId = "${spring.kafka.consumer.group-id}")
//    public void listen(Message<SisUser> sisUserMessage) {
//        System.out.println("Received message: " + sisUserMessage.getPayload());
//    }
//
//    @KafkaListener(topics = "${kafka.user-topic}", containerFactory = "kafkaListenerContainerFactory", groupId = "${spring.kafka.consumer.group-id}")
//    public void listenUser(Message<SisUser> sisUserMessage) {
//        System.out.println("Received user message: " + sisUserMessage.getPayload());
//    }

}