spring:
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      acks: all
      retries: 3
  application:
    name: student-service-test

fulfilment:
  service:
    url: http://localhost:8087

logging:
  level:
    com.example.dcs: DEBUG
    org.apache.kafka: WARN

